#ifndef EIGEN_ARRAY_MODULE_H
#define EIGEN_ARRAY_MODULE_H

// include Core first to handle Eigen2 support macros
#include "Core"

// The old Array module has been merged into Core in Eigen3.
// We keep this header for compatibility but no longer issue an error.

#endif // EIGEN_ARRAY_MODULE_H
